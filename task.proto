syntax  = "proto2";

package PS;

message Task {
	optional string id = 1;
	optional string ip = 2;
	optional int32 port = 3;

	//actually, above information can be obtained from socket

	//something useful
	
}
